const numbers = [1, 2, 3];

/* join array elements into a string by using join() method, it takes a string as a optional parameter, the string will be used as a separator between elements, here i used a comma (,) */
const joined = numbers.join(',');
console.log(joined);
